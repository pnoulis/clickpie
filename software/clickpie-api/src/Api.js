class Api {
  constructor({ clickupClient, webhookUrl } = {}) {
    this.clickupClient = clickupClient;
    this.webhookUrl = webhookUrl;
  }
  getWorkspaces() {};
  createHook() {
  };
}

export { Api };
